import 'package:flutter/material.dart';

class WorkHistoryListScreen extends StatelessWidget {
  const WorkHistoryListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Work History')),
      body: const Center(child: Text('Work History List Screen (Placeholder)')),
    );
  }
}
