import 'package:flutter/material.dart';

class MachinesListScreen extends StatelessWidget {
  const MachinesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Machines List')),
      body: const Center(child: Text('Machines List Screen (Placeholder)')),
    );
  }
}
