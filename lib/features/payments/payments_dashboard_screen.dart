import 'package:flutter/material.dart';

class PaymentsDashboardScreen extends StatelessWidget {
  const PaymentsDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Payments Dashboard')),
      body: const Center(child: Text('Payments Dashboard Screen (Placeholder)')),
    );
  }
}
